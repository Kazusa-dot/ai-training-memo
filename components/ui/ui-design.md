# AI筋トレメモアプリ - UI/UX設計書

**作成日**: 2026-01-30  
**参考アプリ**: Strong, FitNotes  
**デザイン方針**: 既存の記録アプリUI + AIアシスタント統合

---

## 1. デザイン哲学

### コアコンセプト
**「記録は素早く、分析はAIに任せる」**

- **効率性**: 3タップ以内でトレーニング記録完了
- **視認性**: ジムの環境下でも見やすい大きな文字とコントラスト
- **一貫性**: 既存アプリのUXパターンを踏襲（学習コスト削減）
- **AI統合**: 押し付けがましくないが、価値を感じられる

### ユーザー体験の原則
1. **記録が最優先**: 記録画面へのアクセスは常に1タップ
2. **邪魔しない**: AIは記録の邪魔をしない（保存後に表示）
3. **文脈に沿う**: AIは必要な時に必要な情報を提供
4. **オプトアウト可能**: AIが不要な人は無視できる

---

## 2. カラーパレット & ビジュアルデザイン

### カラースキーム（ダークテーマ）

```css
/* Primary Colors */
--background: #0A0E14;        /* 深い黒 */
--surface: #13171F;           /* カード背景 */
--surface-elevated: #1C222E;  /* モーダル等 */

/* Accent Colors */
--primary: #00E5FF;           /* 電動シアン - メインアクション */
--primary-hover: #00B8CC;     /* ホバー状態 */
--secondary: #0EA5E9;         /* スカイブルー - セカンダリ */

/* Text Colors */
--text-primary: #FFFFFF;      /* メインテキスト */
--text-secondary: #94A3B8;    /* サブテキスト */
--text-tertiary: #64748B;     /* 補助テキスト */

/* Semantic Colors */
--success: #10B981;           /* 成功・成長 */
--warning: #F59E0B;           /* 注意 */
--error: #EF4444;             /* エラー */
--info: #3B82F6;              /* 情報 */

/* AI Specific */
--ai-gradient-start: #00E5FF; /* AIカードの背景グラデーション */
--ai-gradient-end: #0EA5E9;
--ai-border: #00E5FF;         /* AIセクションのボーダー */
```

### タイポグラフィ

```css
/* Font Family */
--font-primary: 'Inter', -apple-system, sans-serif;
--font-mono: 'JetBrains Mono', monospace; /* 数値表示用 */

/* Font Sizes (モバイル最適化) */
--text-xs: 0.75rem;    /* 12px - ラベル */
--text-sm: 0.875rem;   /* 14px - 本文小 */
--text-base: 1rem;     /* 16px - 本文 */
--text-lg: 1.125rem;   /* 18px - 見出し小 */
--text-xl: 1.25rem;    /* 20px - 見出し */
--text-2xl: 1.5rem;    /* 24px - 大見出し */
--text-3xl: 1.875rem;  /* 30px - タイトル */

/* Font Weights */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

### スペーシング（8pxベース）

```css
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.25rem;   /* 20px */
--space-6: 1.5rem;    /* 24px */
--space-8: 2rem;      /* 32px */
--space-10: 2.5rem;   /* 40px */
```

### ボーダー半径

```css
--radius-sm: 0.375rem;  /* 6px - 小要素 */
--radius-md: 0.5rem;    /* 8px - カード */
--radius-lg: 0.75rem;   /* 12px - モーダル */
--radius-xl: 1rem;      /* 16px - 大きな要素 */
--radius-full: 9999px;  /* 完全な円形 */
```

---

## 3. 画面構成とナビゲーション

### アプリ全体構造

```
┌─────────────────────────────┐
│     Tab Navigation          │ ← 下部固定
│  [記録] [履歴] [統計] [設定]  │
└─────────────────────────────┘
```

### タブナビゲーション（下部固定）

```
┌────────────────────────────────────────┐
│ 🏋️ 記録 │ 📋 履歴 │ 📊 統計 │ ⚙️ 設定 │
└────────────────────────────────────────┘
```

**各タブの役割:**
- **記録（ホーム）**: 今日のトレーニングを記録
- **履歴**: 過去のワークアウト一覧
- **統計**: グラフと進捗（Phase 2で追加）
- **設定**: ユーザー設定、目標、テンプレート

---

## 4. 画面設計詳細

### 4.1 記録画面（メインスクリーン）

**デザイン方針**: Strongアプリのシンプルさ + FitNotesの効率性

```
┌──────────────────────────────────┐
│ ← 2026年1月30日（木）        ⋮  │ ← ヘッダー
├──────────────────────────────────┤
│                                  │
│  [+ 種目を追加]                   │ ← 空の状態
│                                  │
│  💡 今日のトレーニングを           │
│     記録しましょう                 │
│                                  │
└──────────────────────────────────┘
```

#### 種目追加後の表示

```
┌──────────────────────────────────┐
│ ← 2026年1月30日（木）        ⋮  │
├──────────────────────────────────┤
│                                  │
│ ┌────────────────────────────┐  │
│ │ ベンチプレス            ⋮  │  │ ← 種目カード
│ │ 胸                         │  │
│ │                            │  │
│ │ ┌─────┬──────┬─────┬───┐ │  │
│ │ │SET  │ kg   │ reps│ ✓ │ │  │ ← テーブルヘッダー
│ │ ├─────┼──────┼─────┼───┤ │  │
│ │ │  1  │ 60.0 │  10 │ ✓ │ │  │ ← セット行
│ │ ├─────┼──────┼─────┼───┤ │  │
│ │ │  2  │ 60.0 │   9 │ ✓ │ │  │
│ │ ├─────┼──────┼─────┼───┤ │  │
│ │ │  3  │ 60.0 │   8 │ ✓ │ │  │
│ │ ├─────┼──────┼─────┼───┤ │  │
│ │ │  4  │      │     │   │ │  │ ← 空行（次セット）
│ │ └─────┴──────┴─────┴───┘ │  │
│ │                            │  │
│ │ 📝 前回: 60kg × 3セット      │  │ ← 前回の記録
│ │    合計: 1710kg             │  │
│ └────────────────────────────┘  │
│                                  │
│ [+ 種目を追加]                   │
│                                  │
├──────────────────────────────────┤
│ [💬 AIに相談]    [✓ 完了して保存]│ ← フッターアクション
└──────────────────────────────────┘
```

#### UI要素の詳細

**種目カード:**
- 背景: `--surface`
- ボーダー: 1px solid rgba(255,255,255,0.1)
- 角丸: `--radius-lg`
- パディング: `--space-4`

**セット入力テーブル:**
- ヘッダー背景: `--surface-elevated`
- 行高さ: 48px（タップしやすい）
- フォント: `--font-mono`（数値が揃う）
- フォーカス時: シアンのボーダー

**チェックマーク:**
- 未完了: グレーの円
- 完了: シアンの✓
- タップでトグル

**前回の記録:**
- テキスト色: `--text-tertiary`
- フォントサイズ: `--text-sm`
- アイコン: 📝

---

### 4.2 種目選択画面（モーダル）

```
┌──────────────────────────────────┐
│ 種目を選択              ✕        │ ← モーダルヘッダー
├──────────────────────────────────┤
│ 🔍 種目を検索...                  │ ← 検索バー
├──────────────────────────────────┤
│                                  │
│ 胸 ▼                             │ ← カテゴリ
│ ┌────────────────────────────┐  │
│ │ ベンチプレス                │  │
│ │ インクラインプレス          │  │
│ │ ダンベルフライ              │  │
│ └────────────────────────────┘  │
│                                  │
│ 背中 ▼                           │
│ ┌────────────────────────────┐  │
│ │ デッドリフト                │  │
│ │ ラットプルダウン            │  │
│ │ ベントオーバーロー          │  │
│ └────────────────────────────┘  │
│                                  │
│ 脚 ▼                             │
│ ...                              │
│                                  │
│ [+ カスタム種目を作成]            │
└──────────────────────────────────┘
```

**インタラクション:**
- タップで選択 → モーダル閉じる → 記録画面に種目追加
- 検索: リアルタイムフィルタリング
- カテゴリ: アコーディオン形式

---

### 4.3 AI分析結果画面

**表示タイミング**: ワークアウト保存直後

```
┌──────────────────────────────────┐
│            AI分析中...            │ ← ローディング
│     ⚡ データを分析しています      │
└──────────────────────────────────┘

        ↓ 3-5秒後

┌──────────────────────────────────┐
│ ✨ AI分析結果                      │
├──────────────────────────────────┤
│                                  │
│ ┌────────────────────────────┐  │
│ │  🤖 パーソナルトレーナー    │  │ ← AIカード
│ │                            │  │   グラデーション背景
│ │  前回より総ボリュームが12%  │  │
│ │  増えてます💪 重量はそのまま │  │
│ │  でレップ数が全体的に伸びて  │  │
│ │  いて理想的な成長です。次回  │  │
│ │  は最後のセットだけ62.5kg  │  │
│ │  にチャレンジしてみましょう。│  │
│ │  この調子です！             │  │
│ │                            │  │
│ │  📊 詳細を見る →           │  │
│ └────────────────────────────┘  │
│                                  │
│ 今日のサマリー                    │
│ ┌────────────────────────────┐  │
│ │ 種目数     2種目            │  │
│ │ 総セット数  9セット          │  │
│ │ 総ボリューム 3,240kg        │  │
│ │ 所要時間    45分            │  │
│ └────────────────────────────┘  │
│                                  │
├──────────────────────────────────┤
│ [ホームに戻る]     [履歴を見る]   │
└──────────────────────────────────┘
```

#### AIカードのデザイン詳細

```css
.ai-card {
  background: linear-gradient(135deg, 
    rgba(0, 229, 255, 0.1) 0%, 
    rgba(14, 165, 233, 0.1) 100%
  );
  border: 1px solid var(--ai-border);
  border-radius: var(--radius-lg);
  padding: var(--space-5);
  position: relative;
  overflow: hidden;
}

.ai-card::before {
  /* グラデーションアニメーション用 */
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(
    circle, 
    rgba(0, 229, 255, 0.2) 0%, 
    transparent 50%
  );
  animation: pulse 3s ease-in-out infinite;
}
```

---

### 4.4 履歴画面

```
┌──────────────────────────────────┐
│ 履歴                    🔍      │ ← ヘッダー
├──────────────────────────────────┤
│                                  │
│ 📅 今週                          │
│                                  │
│ ┌────────────────────────────┐  │
│ │ 1月30日（木）              │  │ ← ワークアウトカード
│ │ ベンチプレス、スクワット    │  │
│ │ 9セット · 3,240kg         │  │
│ │                            │  │
│ │ 🤖 前回より12%増えてます... │  │ ← AI要約
│ └────────────────────────────┘  │
│                                  │
│ ┌────────────────────────────┐  │
│ │ 1月28日（火）              │  │
│ │ デッドリフト、懸垂          │  │
│ │ 7セット · 2,880kg         │  │
│ └────────────────────────────┘  │
│                                  │
│ 📅 先週                          │
│                                  │
│ ┌────────────────────────────┐  │
│ │ 1月23日（木）              │  │
│ │ ベンチプレス、ディップス    │  │
│ │ 9セット · 2,910kg         │  │
│ └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

**インタラクション:**
- カードタップ → 詳細画面へ
- 検索: 種目名で絞り込み
- 日付でグルーピング（今日、今週、先週、今月、...）

---

### 4.5 ワークアウト詳細画面

```
┌──────────────────────────────────┐
│ ← 1月30日（木）            ⋮    │
├──────────────────────────────────┤
│                                  │
│ ┌────────────────────────────┐  │
│ │ ベンチプレス                │  │
│ │ ┌─────┬──────┬─────┐      │  │
│ │ │SET  │ kg   │ reps│      │  │
│ │ ├─────┼──────┼─────┤      │  │
│ │ │  1  │ 60.0 │  10 │      │  │
│ │ │  2  │ 60.0 │   9 │      │  │
│ │ │  3  │ 60.0 │   8 │      │  │
│ │ └─────┴──────┴─────┘      │  │
│ │ 合計: 1,620kg              │  │
│ └────────────────────────────┘  │
│                                  │
│ ┌────────────────────────────┐  │
│ │ スクワット                  │  │
│ │ ┌─────┬──────┬─────┐      │  │
│ │ │  1  │ 80.0 │  10 │      │  │
│ │ │  2  │ 80.0 │   9 │      │  │
│ │ │  3  │ 80.0 │   8 │      │  │
│ │ └─────┴──────┴─────┘      │  │
│ │ 合計: 1,620kg              │  │
│ └────────────────────────────┘  │
│                                  │
│ ┌────────────────────────────┐  │
│ │  🤖 AI分析                  │  │
│ │                            │  │
│ │  前回より総ボリュームが12%  │  │
│ │  増えてます💪 ...          │  │
│ │                            │  │
│ │  [詳しく見る]               │  │
│ └────────────────────────────┘  │
│                                  │
│ 📝 メモ                          │
│ 今日は調子が良かった              │
│                                  │
├──────────────────────────────────┤
│ [編集]         [削除]            │
└──────────────────────────────────┘
```

---

### 4.6 統計画面（Phase 2）

```
┌──────────────────────────────────┐
│ 統計                    🔍      │
├──────────────────────────────────┤
│                                  │
│ 期間: [1週間] [1ヶ月] [3ヶ月]    │
│                                  │
│ ┌────────────────────────────┐  │
│ │ 📈 総ボリューム推移          │  │
│ │                            │  │
│ │     ╱╲                    │  │
│ │    ╱  ╲╱╲                │  │
│ │   ╱       ╲╱             │  │
│ │  ╱                        │  │
│ │ ─────────────────────────│  │
│ │  1/7  1/14  1/21  1/28    │  │
│ │                            │  │
│ │ 今月: +15% ↗              │  │
│ └────────────────────────────┘  │
│                                  │
│ 種目別ベスト                      │
│ ┌────────────────────────────┐  │
│ │ ベンチプレス    62.5kg ✨   │  │
│ │ スクワット      100kg       │  │
│ │ デッドリフト    120kg       │  │
│ └────────────────────────────┘  │
│                                  │
│ 🤖 今月のAIレポート               │
│ ┌────────────────────────────┐  │
│ │ 今月はベンチプレスのMAXが    │  │
│ │ 5kg更新されました。上腕三頭筋│  │
│ │ の種目が少ないので、追加する │  │
│ │ と胸トレの効果が上がるかも    │  │
│ │                            │  │
│ │ [詳しく見る]                │  │
│ └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

---

### 4.7 AIチャット画面（オプション機能）

**アクセス**: 記録画面下部の「💬 AIに相談」ボタン

```
┌──────────────────────────────────┐
│ ← AIトレーナー                    │
├──────────────────────────────────┤
│                                  │
│ ┌────────────────────────────┐  │
│ │ 🤖                          │  │ ← AIメッセージ
│ │ こんにちは！今日のトレーニング│  │
│ │ について何か質問はありますか？│  │
│ └────────────────────────────┘  │
│                                  │
│              ┌─────────────────┐│
│              │ ベンチプレスの   ││ ← ユーザーメッセージ
│              │ 重量を上げるべき？││
│              └─────────────────┘│
│                                  │
│ ┌────────────────────────────┐  │
│ │ 🤖                          │  │
│ │ 前回と同じ60kgで全セット完遂 │  │
│ │ できているので、次回は62.5kg│  │
│ │ にチャレンジしてみましょう！  │  │
│ └────────────────────────────┘  │
│                                  │
│                                  │
├──────────────────────────────────┤
│ 💬 メッセージを入力...      [送信]│
└──────────────────────────────────┘
```

**クイック質問ボタン:**
```
┌──────────────────────────────────┐
│ よくある質問:                     │
│ [次回の重量は?] [頻度は十分?]     │
│ [休養日が必要?] [フォームは?]     │
└──────────────────────────────────┘
```

---

### 4.8 設定画面

```
┌──────────────────────────────────┐
│ ← 設定                           │
├──────────────────────────────────┤
│                                  │
│ プロフィール                      │
│ ┌────────────────────────────┐  │
│ │ 👤 [名前]                   │  │
│ │ 📧 email@example.com        │  │
│ └────────────────────────────┘  │
│                                  │
│ 単位設定                          │
│ ┌────────────────────────────┐  │
│ │ 重量: [kg] / lb             │  │
│ │ 距離: [km] / mi             │  │
│ └────────────────────────────┘  │
│                                  │
│ AI設定                            │
│ ┌────────────────────────────┐  │
│ │ ✅ トレーニング後に自動分析  │  │
│ │ ✅ 週次レポートを送信        │  │
│ │ アドバイスの詳しさ:         │  │
│ │ ○ 簡潔  ● 普通  ○ 詳細    │  │
│ └────────────────────────────┘  │
│                                  │
│ テンプレート（Phase 2）           │
│ ┌────────────────────────────┐  │
│ │ 胸の日                      │  │
│ │ 脚の日                      │  │
│ │ 背中の日                    │  │
│ │ [+ 新規作成]                │  │
│ └────────────────────────────┘  │
│                                  │
│ 目標（Phase 2）                   │
│ ┌────────────────────────────┐  │
│ │ ベンチプレス 100kg達成      │  │
│ │ 進捗: ████░░░░ 62.5kg/100kg│  │
│ │ [+ 新しい目標]              │  │
│ └────────────────────────────┘  │
│                                  │
│ データ                            │
│ ┌────────────────────────────┐  │
│ │ エクスポート                │  │
│ │ 同期状態                    │  │
│ └────────────────────────────┘  │
│                                  │
│ アカウント                        │
│ ┌────────────────────────────┐  │
│ │ ログアウト                  │  │
│ └────────────────────────────┘  │
│                                  │
└──────────────────────────────────┘
```

---

## 5. コンポーネント設計

### 5.1 共通コンポーネント

#### Button（ボタン）

```typescript
interface ButtonProps {
  variant: 'primary' | 'secondary' | 'ghost' | 'danger'
  size: 'sm' | 'md' | 'lg'
  fullWidth?: boolean
  icon?: ReactNode
  loading?: boolean
  disabled?: boolean
  onClick: () => void
  children: ReactNode
}
```

**バリエーション:**
```tsx
// Primary: メインアクション
<Button variant="primary">保存</Button>

// Secondary: サブアクション
<Button variant="secondary">キャンセル</Button>

// Ghost: 控えめなアクション
<Button variant="ghost">編集</Button>

// Danger: 削除等
<Button variant="danger">削除</Button>
```

**スタイル:**
```css
/* Primary */
.button-primary {
  background: var(--primary);
  color: var(--background);
  font-weight: var(--font-semibold);
}

.button-primary:hover {
  background: var(--primary-hover);
}

.button-primary:active {
  transform: scale(0.98);
}
```

---

#### Card（カード）

```typescript
interface CardProps {
  variant?: 'default' | 'ai' | 'elevated'
  padding?: 'sm' | 'md' | 'lg'
  onClick?: () => void
  children: ReactNode
}
```

**AIカード特殊スタイル:**
```tsx
<Card variant="ai">
  <div className="flex items-start gap-3">
    <div className="ai-icon">🤖</div>
    <div className="ai-content">
      {aiMessage}
    </div>
  </div>
</Card>
```

---

#### Input（入力フィールド）

```typescript
interface InputProps {
  type: 'text' | 'number' | 'search'
  placeholder?: string
  value: string | number
  onChange: (value: string | number) => void
  suffix?: ReactNode // 単位表示等
  error?: string
  autoFocus?: boolean
}
```

**重量入力の例:**
```tsx
<Input
  type="number"
  value={weight}
  onChange={setWeight}
  suffix="kg"
  autoFocus
/>
```

**スタイル:**
```css
.input {
  background: var(--surface-elevated);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-md);
  padding: var(--space-3) var(--space-4);
  font-family: var(--font-mono);
  font-size: var(--text-lg);
  color: var(--text-primary);
}

.input:focus {
  border-color: var(--primary);
  outline: none;
  box-shadow: 0 0 0 3px rgba(0, 229, 255, 0.1);
}
```

---

#### ExerciseCard（種目カード）

```typescript
interface ExerciseCardProps {
  exercise: {
    name: string
    category: string
    sets: Set[]
  }
  previousData?: {
    weight: number
    volume: number
    sets: number
  }
  onAddSet: () => void
  onUpdateSet: (setId: string, data: Partial<Set>) => void
  onRemoveSet: (setId: string) => void
  onRemove: () => void
}
```

**構造:**
```tsx
<ExerciseCard>
  <ExerciseHeader>
    {name} · {category}
  </ExerciseHeader>
  
  <SetsTable>
    {sets.map(set => (
      <SetRow
        key={set.id}
        set={set}
        onUpdate={onUpdateSet}
      />
    ))}
  </SetsTable>
  
  {previousData && (
    <PreviousDataDisplay data={previousData} />
  )}
</ExerciseCard>
```

---

#### SetRow（セット行）

```typescript
interface SetRowProps {
  set: Set
  onUpdate: (data: Partial<Set>) => void
  onComplete: () => void
}
```

**レイアウト:**
```tsx
<div className="set-row">
  <span className="set-number">{setNumber}</span>
  <Input
    type="number"
    value={weight}
    onChange={value => onUpdate({ weight: value })}
    suffix="kg"
  />
  <Input
    type="number"
    value={reps}
    onChange={value => onUpdate({ reps: value })}
  />
  <Checkbox
    checked={completed}
    onChange={onComplete}
  />
</div>
```

---

#### AICard（AI分析カード）

```typescript
interface AICardProps {
  message: string
  loading?: boolean
  onViewDetails?: () => void
}
```

**デザイン:**
```tsx
<div className="ai-card">
  {loading ? (
    <div className="ai-loading">
      <Spinner />
      <span>分析中...</span>
    </div>
  ) : (
    <>
      <div className="ai-header">
        <span className="ai-icon">🤖</span>
        <span className="ai-title">パーソナルトレーナー</span>
      </div>
      <p className="ai-message">{message}</p>
      {onViewDetails && (
        <Button variant="ghost" onClick={onViewDetails}>
          詳細を見る →
        </Button>
      )}
    </>
  )}
</div>
```

---

### 5.2 レイアウトコンポーネント

#### AppLayout（全体レイアウト）

```tsx
<AppLayout>
  <Header>
    <BackButton />
    <Title />
    <Actions />
  </Header>
  
  <Content>
    {children}
  </Content>
  
  <TabNavigation>
    <Tab icon="🏋️" label="記録" active />
    <Tab icon="📋" label="履歴" />
    <Tab icon="📊" label="統計" />
    <Tab icon="⚙️" label="設定" />
  </TabNavigation>
</AppLayout>
```

---

## 6. インタラクションとアニメーション

### 6.1 トランジション

```css
/* ページ遷移 */
.page-enter {
  opacity: 0;
  transform: translateX(20px);
}

.page-enter-active {
  opacity: 1;
  transform: translateX(0);
  transition: all 0.3s ease-out;
}

.page-exit {
  opacity: 1;
  transform: translateX(0);
}

.page-exit-active {
  opacity: 0;
  transform: translateX(-20px);
  transition: all 0.3s ease-in;
}
```

### 6.2 ボタンインタラクション

```css
.button {
  transition: all 0.2s ease;
}

.button:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(0, 229, 255, 0.2);
}

.button:active {
  transform: scale(0.98);
}
```

### 6.3 カードホバー

```css
.workout-card {
  transition: all 0.2s ease;
  cursor: pointer;
}

.workout-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
  border-color: var(--primary);
}
```

### 6.4 AI分析ローディング

```tsx
const AILoadingAnimation = () => (
  <div className="ai-loading">
    <div className="pulse-ring" />
    <span className="ai-icon">🤖</span>
    <p>データを分析しています...</p>
  </div>
)
```

```css
@keyframes pulse-ring {
  0% {
    transform: scale(0.95);
    opacity: 1;
  }
  50% {
    transform: scale(1.05);
    opacity: 0.7;
  }
  100% {
    transform: scale(0.95);
    opacity: 1;
  }
}

.pulse-ring {
  position: absolute;
  width: 100%;
  height: 100%;
  border: 2px solid var(--primary);
  border-radius: 50%;
  animation: pulse-ring 2s ease-in-out infinite;
}
```

---

## 7. レスポンシブ対応

### ブレークポイント

```css
/* モバイル優先 */
--mobile: 0px;       /* デフォルト */
--tablet: 768px;     /* 使用しない（モバイルのみ） */
--desktop: 1024px;   /* 使用しない */
```

**注意**: このアプリはモバイル専用のため、タブレット・デスクトップ対応は不要

### 画面サイズ最適化

```css
/* 小さい画面（iPhone SE等） */
@media (max-width: 375px) {
  :root {
    --text-base: 0.9375rem; /* 15px */
    --space-4: 0.875rem;    /* 14px */
  }
}

/* 大きい画面（iPhone Pro Max等） */
@media (min-width: 428px) {
  .content {
    max-width: 428px;
    margin: 0 auto;
  }
}
```

---

## 8. アクセシビリティ

### タップターゲット

```css
/* 最小44x44px（Appleガイドライン） */
.button,
.checkbox,
.tab-item {
  min-height: 44px;
  min-width: 44px;
}

/* 入力フィールドは大きめに */
.input,
.set-row input {
  min-height: 48px;
  font-size: 16px; /* iOS ズーム防止 */
}
```

### カラーコントラスト

```css
/* WCAG AA準拠 */
/* 白文字 on ダーク背景: 15:1以上 */
--text-primary: #FFFFFF;  /* on #0A0E14 */

/* グレー文字: 7:1以上 */
--text-secondary: #94A3B8;

/* シアン: 十分なコントラスト */
--primary: #00E5FF;
```

### フォーカス表示

```css
*:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
}

/* インプットのフォーカス */
.input:focus {
  box-shadow: 0 0 0 3px rgba(0, 229, 255, 0.2);
}
```

---

## 9. エラー状態とフィードバック

### トースト通知

```tsx
<Toast variant="success">
  ✓ トレーニングを保存しました
</Toast>

<Toast variant="error">
  ✗ 保存に失敗しました
</Toast>

<Toast variant="info">
  ℹ オフラインモードです
</Toast>
```

**デザイン:**
```css
.toast {
  position: fixed;
  bottom: 80px; /* タブナビゲーションの上 */
  left: 50%;
  transform: translateX(-50%);
  padding: var(--space-4) var(--space-5);
  border-radius: var(--radius-lg);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
  animation: toast-slide-up 0.3s ease-out;
}

@keyframes toast-slide-up {
  from {
    opacity: 0;
    transform: translate(-50%, 20px);
  }
  to {
    opacity: 1;
    transform: translate(-50%, 0);
  }
}
```

### 空の状態

```tsx
<EmptyState
  icon="🏋️"
  title="まだトレーニング記録がありません"
  description="今日のトレーニングを記録しましょう"
  action={
    <Button onClick={onStartWorkout}>
      記録を始める
    </Button>
  }
/>
```

---

## 10. パフォーマンス最適化

### 画像最適化

```tsx
// アイコンはSVGを使用
import BenchPressIcon from './icons/bench-press.svg'

// 必要に応じてWebP
<img 
  src="chart.webp" 
  alt="Progress chart"
  loading="lazy"
/>
```

### 仮想化（Phase 2: 統計画面）

```tsx
import { VirtualList } from 'react-virtual'

<VirtualList
  data={workoutHistory}
  itemHeight={80}
  renderItem={(workout) => (
    <WorkoutCard workout={workout} />
  )}
/>
```

### Lazy Loading

```tsx
// 統計画面は遅延ロード
const StatsScreen = lazy(() => import('./screens/Stats'))

<Suspense fallback={<Spinner />}>
  <StatsScreen />
</Suspense>
```

---

## 11. フォルダ構成

```
src/
├── components/
│   ├── ui/                    # 共通UIコンポーネント
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   ├── Checkbox.tsx
│   │   ├── Modal.tsx
│   │   └── Toast.tsx
│   │
│   ├── workout/               # トレーニング関連
│   │   ├── ExerciseCard.tsx
│   │   ├── SetRow.tsx
│   │   ├── ExerciseSelector.tsx
│   │   └── WorkoutSummary.tsx
│   │
│   ├── ai/                    # AI関連
│   │   ├── AICard.tsx
│   │   ├── AIChat.tsx
│   │   └── AILoadingAnimation.tsx
│   │
│   └── layout/                # レイアウト
│       ├── AppLayout.tsx
│       ├── Header.tsx
│       └── TabNavigation.tsx
│
├── screens/                   # 画面コンポーネント
│   ├── WorkoutScreen.tsx      # 記録画面
│   ├── HistoryScreen.tsx      # 履歴画面
│   ├── StatsScreen.tsx        # 統計画面（Phase 2）
│   ├── SettingsScreen.tsx     # 設定画面
│   └── WorkoutDetailScreen.tsx
│
├── hooks/                     # カスタムフック
│   ├── useWorkout.ts
│   ├── useAI.ts
│   ├── useOfflineSync.ts
│   └── useToast.ts
│
├── lib/                       # ユーティリティ
│   ├── supabase.ts
│   ├── ai-client.ts
│   └── storage.ts
│
├── store/                     # Zustand ストア
│   ├── workoutStore.ts
│   ├── userStore.ts
│   └── syncStore.ts
│
├── types/                     # TypeScript型定義
│   ├── workout.ts
│   ├── exercise.ts
│   └── ai.ts
│
└── styles/                    # グローバルスタイル
    ├── globals.css
    ├── variables.css
    └── animations.css
```

---

## 12. 実装の優先順位

### Phase 1: MVP（Week 1-3）

1. **Week 1**:
   - [ ] 基本レイアウト（AppLayout, TabNavigation）
   - [ ] 認証画面（ログイン/サインアップ）
   - [ ] 共通コンポーネント（Button, Input, Card）

2. **Week 2**:
   - [ ] 記録画面UI（ExerciseCard, SetRow）
   - [ ] 種目選択モーダル
   - [ ] 履歴画面（一覧表示）

3. **Week 3**:
   - [ ] AI分析結果画面
   - [ ] AIカードコンポーネント
   - [ ] トースト通知

### Phase 2: 機能強化（Week 4-6）

4. **Week 4-5**:
   - [ ] 統計画面（グラフ）
   - [ ] データ可視化コンポーネント

5. **Week 6**:
   - [ ] 設定画面
   - [ ] テンプレート機能
   - [ ] 目標設定UI

---

## 13. デザインシステム活用

### shadcn/ui のカスタマイズ

```bash
# 必要なコンポーネントをインストール
npx shadcn-ui@latest add button
npx shadcn-ui@latest add card
npx shadcn-ui@latest add input
npx shadcn-ui@latest add dialog
```

**tailwind.config.js にテーマ設定:**

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        background: '#0A0E14',
        surface: '#13171F',
        primary: '#00E5FF',
        'primary-hover': '#00B8CC',
        // ...
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
    },
  },
}
```

---

## 14. まとめ

このUI設計書は以下を実現します：

✅ **既存アプリ（Strong/FitNotes）の優れたUXパターンを踏襲**
✅ **AI機能を自然に統合**（押し付けがましくない）
✅ **モバイルファースト**（ジムでの使いやすさ優先）
✅ **ダークテーマ + 電動ブルーアクセント**
✅ **効率的な記録フロー**（3タップ以内で完了）
✅ **明確なコンポーネント構造**

次のステップ: この設計書に基づいて、実際のコンポーネントコードを生成していきます！
